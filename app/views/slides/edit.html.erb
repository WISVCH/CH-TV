<h1>Editing Slide</h1>

<div class="slide_fullwidth">
  <%= image_tag @slide.content.image(:medium) %>
</div>

<%= render 'form', slide: @slide %>

<%= link_to "delete", @slide, method: :delete, data: { confirm: "Are you sure?" } %>
